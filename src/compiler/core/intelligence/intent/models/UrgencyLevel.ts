// ─── Urgency level (re-exported) ───────────────────────────────────────────────
// Re-exported from Context Intelligence to keep a single canonical definition.
// Values: 'low' | 'normal' | 'high' | 'critical'.

export type { Urgency } from '../../models/BusinessContext';
